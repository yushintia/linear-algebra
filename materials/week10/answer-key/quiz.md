## Answer Key

1. **B.** `Av = λv` is the defining equation for an eigenvector and its eigenvalue.
2. **C.** `v = 0` always solves the equation but carries no information; it is the trivial solution.
3. **B.** A nonzero `v` can only exist when `A - λI` is not invertible, which means its determinant is 0.
4. **C.** `5` is the eigenvalue; `v` is the eigenvector.
5. **C.** David Hilbert named it *eigenwert* in 1904.
6. **B.** PageRank repeatedly redistributes importance across links until the split stops changing — an eigenvector.
7. **Yes, eigenvalue 1.** `(2, -2)` is `(1, -1)` scaled by 2. `A(2,-2) = (2·2+1·(-2), 1·2+2·(-2)) = (2, -2) = 1(2, -2)`. Check: `2 - 2 = 0` and the result matches the input exactly, confirming `λ = 1`.
